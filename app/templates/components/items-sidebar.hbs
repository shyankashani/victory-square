<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      {{#each colors as |color|}}
        <div {{action toggleColorId color.id}}>
          {{color.name}}
        </div>
      {{/each}}
    </div>
  </div>
</div>
