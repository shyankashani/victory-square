{{#sidebar-container as |section|}}
  {{#if section.isHeaderSection}}
    Game Filters
  {{else if section.isBodySection}}
    {{#info-label header="Number of Players" icon="users"}}
      {{filter-checkbox text="1" color="dark"}}
    {{/info-label}}
  {{/if}}
{{/sidebar-container}}
